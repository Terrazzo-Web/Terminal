name: Test client
description: "Run tests for client components."
runs:
  using: "composite"
  steps:
    - name: Test client
      run: |
        cargo test --locked --no-default-features --features diagnostics,client-all
      shell: bash
