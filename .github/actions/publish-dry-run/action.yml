name: Publish (dry-run)
runs:
  using: "composite"
  steps:
    - name: Publish (dry-run)
      run: |
        cargo publish --locked -p terrazzo-pty --dry-run
        cargo publish --locked -p terrazzo-terminal --dry-run
      shell: bash
