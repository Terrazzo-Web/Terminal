import {
    EditorView,
    basicSetup
} from "codemirror";
import { EditorState } from '@codemirror/state';

// Export them for Webpack to expose as globals
export {
    basicSetup,
    EditorState,
    EditorView,
};
